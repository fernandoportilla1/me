language: node_js
node_js:
  - "12.18.2"
cache: npm

env:
  - PUBLIC_URL=./

install:
  - npm install

script:
  - npm run test
  - npm run build

deploy:
  provider: pages
  cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  local_dir: ./build/
  on:
    branch: master