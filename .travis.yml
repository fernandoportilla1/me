language: node_js
node_js:
  - "12.18.2"
cache: npm

install:
  - npm install

script:
  - npm install run build

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master